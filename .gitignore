# Datasets
datasets*/
datasets_merged*/

# Ignore all runs by default
runs/**

# Re-include runs + report bundle (allow traversal)
!runs/
!runs/report/
!runs/report/**

# (اختياري) اسمحي ببعض الآرتيفاكتات الخفيفة من كل الداتا/الفاريانت/الفولد
!runs/*/
!runs/*/*/
!runs/*/*/fold*/
!runs/*/*/summary_*.json
!runs/*/*/metrics_*.csv
!runs/*/*/fold*/confusion_matrix*.png
!runs/*/*/fold*/tsne.png
!runs/*/*/fold*/classification_report.txt

# Checkpoints & large artifacts
*.pth
